#include <iostream>
using namespace std;
int main() 
{
    int a, b, c, x;
    cout <<"Enter the values of a, b, c: ";
    cin >> a >> b >> c;
    x = a / b - c;
    cout << x;
    return 0;
}